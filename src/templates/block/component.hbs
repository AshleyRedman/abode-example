import React from 'react';

interface IProps {

}

const {{ name }}: React.FC<IProps> = (props): React.ReactElement => {

  return (
    <>This is the {{ name }} block</>
  );
};

export default {{ name }};
